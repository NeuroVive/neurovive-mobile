// App constants
class AppConstants {
  // Set to false to bypass the real smart-pen flow and use mock processing.
  static const bool useRealApplication = true;

  // Set to true to show raw live smart-pen packets while using the real pen.
  static const bool showRawSmartPenPackets = false;

  // Set to false to bypass the backend auth flow and use local mock auth.
  static const bool useRealAuth = false;
}
